{
  "name": "wp-theme-health-tests",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.55.1"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  }
}
